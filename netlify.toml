[build]
  # Install root deps (no lockfile), client deps with lockfile, then build client
  command = "npm install && npm --prefix client ci && npm run build"
  publish = "client/build"
  functions = "netlify/functions"

[functions]
  # Keep heavy native modules external to avoid bundling issues
  node_bundler = "esbuild"
  external_node_modules = ["pg", "pdf-parse"]

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/server/api/:splat"
  status = 200
